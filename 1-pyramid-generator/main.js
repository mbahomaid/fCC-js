let character = 'Hello';
let count = 8;

// Add values to the array
let rows = ["Naomi", "Quincy", "CamperChan"];

// Use .length to access the last element in the array
rows[rows.length - 1] = 10;

// Print the first value of the array
console.log(rows)